defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: tbw_dev

test:
  <<: *defaults
  database: tbw_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: tbw
  url: <%= ENV['MONGOHQ_URL'] %>